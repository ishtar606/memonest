# MemoNest 기능 개선 로드맵

> 개인 + 업무 통합 스케줄러로서의 성장 방향. 우선순위별 정리.
> 최종 갱신: 2026-09-18 (v3.0.0 정식 이후)

---

## ✅ 완료

**인프라 / 배포**
- Netlify(Node Functions) + Notion 데이터 구조로 전환·배포
- GitHub 자동 배포 연결 (push → 자동 빌드)
- Google OAuth redirect 동적화 + 캘린더 연동

**일정**
- 시간 선택 UX: 10분 단위 + 시작/종료/소요시간 3자 양방향 연동
- 타임존(GMT) 선택: 전체화면 설정(단말 자동 감지 기본) + 등록/수정 개별 선택
- 캘린더/리스트 날짜 버킷팅 선택 타임존 기준 통일

**회의록**
- STT(Groq) + AI(Gemini) 요약 — 프롬프트 개선(창작 방지, 입력 종류 무관 요약)
- 노션 저장 구조 토글화(긴 요약/논의/액션/원본 접기), DB 속성은 미리보기만
- 일정↔회의록 연동(노션 반영 + 회의록탭 반영 + 완료 배지)
- 'AI 재정리' 버튼 — 기존 회의록 재요약(비파괴적)

**홈 / ToDo 연계**
- 홈 대시보드 개편: 오늘 할 일 + 이번 주 일정 최상단, PC 중복 그리드 제거
- ToDo → 일정 변환('일정으로 잡기' 버튼), 일정 화면에 오늘까지 할 일 마감 배너
- 홈 데이터 fetch 최적화(중복 호출 제거)

**웹푸시 알림**
- VAPID 구독/발송 인프라 + 5분 주기 리마인더 스케줄러
- PC · 안드로이드 실사용 검증 완료 (기기별 독립 구독 → 전 기기 발송)

**모바일 안정화**
- 안드로이드 입력 키보드 닫힘 수정(viewport/폰트)
- 노션 DB 생성 타임아웃 시 '복원' 자동 대체(Failed to fetch 해결)

---

## P0 — 다음 우선 (마무리/핵심)

### 1. 아이폰(iOS) 웹푸시 세팅 완료
- 코드는 이미 지원. 아이폰만 홈 화면 추가 후 알림 켜기 절차 남음(실사용 검증)
- 3대(PC/안드로이드/아이폰) 모두 알림 확인으로 웹푸시 완결

### 2. 반복 일정
- 현재 ToDo만 반복(매일/매주/매월). 일정에도 반복 필요 (정기회의 등 업무 필수)
- Notion 일정 DB에 "반복" select 추가 + 캘린더 전개 로직
- 리마인더 스케줄러가 반복 인스턴스도 인지하도록 확장

### 3. 웹푸시 후속 보강
- ToDo 마감 알림도 푸시로 확장 (현재는 일정만)
- "매일 아침 8시 오늘 요약" 같은 요약 알림
- 발송 이력/중복 방지 정교화 (현재 5분 창 단순 매칭)

---

## P1 — 그 다음

### 4. 통합 검색
- 전 모듈 통합 검색(일정·회의록·ToDo·아이디어)
- 업무 히스토리 추적에 유용

### 5. 이메일 리마인더 (업무 채널 확장)
- 웹푸시 외 채널. Netlify Scheduled Function + 메일 API
- "내일 일정 요약" 아침 발송

### 6. 업무/개인 통합 뷰 필터
- 카테고리 기반 업무/개인 토글 필터 (기존 `_catStyle` 활용)
- 홈·일정에서 컨텍스트 전환

---

## P2 — 완성도

### 7. 다크모드
### 8. 오프라인 강화
- 현재 sw.js는 정적 자산만 캐시 → 읽기 데이터 캐시(조회 오프라인 지원)
### 9. 회의록 블록 인라인 편집
- 안건·논의·액션아이템을 앱에서 직접 수정 (현재는 요약/제목/날짜 위주)

---

## 기술 부채 / 리스크 메모

- **Netlify 함수 타임아웃(10초)**: DB 7개 생성이 이를 초과할 수 있음 → 현재는 '복원' 자동 대체로 우회. 근본적으론 생성을 백그라운드 함수로 분리 검토
- **캘린더 주간 경계**: 타임존 버킷은 통일했으나, 극단적 오프셋 차이의 주 경계는 추가 검증 여지
- **단일 테넌트 구조**: 1 Notion 워크스페이스 기준. 멀티유저로 가려면 인증·구독 분리 필요(현재 개인용엔 적합)

---

## 웹푸시 운영 메모

- 구독 저장: Notion "🔔 Push Subscriptions" DB (최초 구독 시 자동 생성)
- 발송: `netlify/functions/reminders.ts` (5분 주기 cron, UTC)
- 필요한 Netlify 환경변수:
  - `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`(mailto:)
  - `PUSH_DB_ID` (최초 구독 후 `/api/push/subscribe` 응답의 pushDbId)
- 플랫폼:
  - Android/데스크톱: 즉시 사용 가능 (검증 완료)
  - iOS 16.4+: 홈 화면에 추가(PWA 설치) 후에만 동작 → 앱이 자동 안내
