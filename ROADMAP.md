# MemoNest 기능 개선 로드맵

> 개인 + 업무 통합 스케줄러로서의 성장 방향. 우선순위별 정리.
> 최종 갱신: 2026-09-17 (v2.5.0)

---

## ✅ 완료 (v2.5.0까지)

- Netlify(Node Functions) + Notion 데이터 구조로 배포
- Google OAuth redirect 동적화 + 캘린더 연동
- 일정 시간 선택 UX: 10분 단위 + 시작/종료/소요시간 3자 양방향 연동
- 일정↔회의록 연동(노션 반영 + 회의록탭 반영 + 완료 배지), 회의록 날짜 자동 채움
- 일정 타임존(GMT) 선택: 전체화면 설정(단말 자동 감지 기본) + 등록/수정 개별 선택
- **웹푸시 알림**: 구독/발송 인프라 + 5분 주기 리마인더 스케줄러 (Android/데스크톱/iOS)

---

## P0 — 다음 우선 (체감 가치 큼)

### 1. 통합 홈 대시보드 강화
- 오늘/이번 주 일정 + 미완료 ToDo + 임박 마감을 한 화면에
- 주간 아젠다 뷰, 오늘 할 일 Top 3
- 업무/개인 색상·카테고리 필터 (기존 `_catStyle` 활용)

### 2. ToDo ↔ 일정 연계
- Due Date 있는 ToDo를 캘린더에 표시
- "이 할 일을 일정으로 잡기" 버튼 (ToDo → Schedule 변환)
- 개인+업무 통합 관점의 핵심 연결고리

### 3. 웹푸시 후속 보강
- 알림 발송 이력/중복 방지 정교화 (현재는 5분 창 기반 단순 매칭)
- ToDo 마감 알림도 푸시로 확장 (현재는 일정만)
- 알림 시간대 커스터마이즈(예: "매일 아침 8시 오늘 요약")

---

## P1 — 그 다음

### 4. 반복 일정
- 현재 ToDo만 반복(매일/매주/매월). 일정에도 반복 추가 (정기회의 등 업무 필수)
- Notion 스키마: 일정 DB에 "반복" select 추가

### 5. 이메일 리마인더 (업무용)
- 웹푸시 외 채널. Netlify Scheduled Function + 메일 API
- "내일 일정 요약" 아침 발송

### 6. 통합 검색/필터
- 전 모듈 통합 검색(일정·회의록·ToDo·아이디어)
- 업무 히스토리 추적

---

## P2 — 완성도

### 7. 다크모드
### 8. 오프라인 강화
- 현재 sw.js는 정적 자산만 캐시 → 읽기 데이터 캐시(조회 오프라인 지원)
### 9. 회의록 블록 편집
- 안건·논의·액션아이템 인라인 수정

---

## 웹푸시 운영 메모 (v2.5.0 구현 기준)

- 구독 저장: Notion "🔔 Push Subscriptions" DB (최초 구독 시 자동 생성)
- 발송: `netlify/functions/reminders.ts` (5분 주기 cron, UTC)
- 필요한 Netlify 환경변수:
  - `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`(mailto:)
  - `PUSH_DB_ID` (최초 구독 후 `/api/push/subscribe` 응답의 pushDbId 값을 등록)
- 플랫폼:
  - Android/데스크톱: 즉시 사용 가능
  - iOS 16.4+: 홈 화면에 추가(PWA 설치) 후에만 동작 → 앱이 자동 안내
